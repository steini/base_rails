== Base Rails app

Basic Rails app with Rails Edge (2.3.0). Don't forget to add a symbolic link
to your local rails (ln -s /path/to/rails vendor/rails).

rspec, rspec-rails, exception_notifier Plugins are installed via git submodules.

capify was executed.

